program simple_3(a)
    var a is integer;
    return integer;

    function isodd(n)
        var n is integer;
        return boolean;
    is
        var i is boolean;
        var j is integer;
    begin
        i := 0 + 1;
        return yes;
    end function isodd;

is
    var i is grid;
begin
    i:=isodd(1);
    if isodd(1) then
        print 3;
    end if
end
